# 照片追溯上传系统 —— 整体梳理与部署总览（最终版 v33）

> 本文档把之前几十个散装版本/补丁包收敛为 **两份干净交付物**：`NAS 包` 和 `App 包`。
> 历史版本（v12~v33 的各色 zip、patch）已全部由本最终版取代，无需再逐个对照。

---

## 一、系统是什么

手机扫码质检照片 → 自动上传到飞牛 NAS 指定目录，并用追溯码（Code 128 等条码）做文件名与检索。

由两端组成：

| 端 | 角色 | 技术 | 交付物 |
|---|---|---|---|
| **NAS 服务端** | 提供网页上传页 + 接收/落盘照片 + 条码解码 + 后 8 位查询 | Node + Express，Docker 容器 `fnnas-photo-uploader`，监听 **3000(HTTPS)** 与 **3080(HTTP 直连)** | `NAS 包` |
| **App 原生壳** | 启动页（填 NAS 地址、系统相机拍照 + zxing 本地解码） + 加载 NAS 上传页，拍照/扫码走**原生相机通道** | Capacitor 8 + @capacitor/camera + @zxing/library，GitHub Actions 云端出 APK | `App 包` |

App **不把 NAS 地址写死**——启动页本地加载，运行时填地址跳转，换 IP/网段不用重打包。

---

## 二、踩过的坑 → 根因 → 最终解法（一图流）

| 现象 | 根因 | 最终解法（已落地在最终版） |
|---|---|---|
| App 装后打不开 / 3080 连不上 | 3080 默认是「301 跳 3000 自签 HTTPS」；旧镜像 server.js 没有 `APP_HTTP` 直连分支 | `docker-compose.yml` 设 `APP_HTTP=1`，server.js 第 901 行走 3080 **HTTP 完整服务**（免证书直连） |
| 手机 Chrome 开 3080 报 ERR_CONNECTION_ABORTED | http 被升级成 https 跳转 | 同上，固化镜像后 3080 直接 200 OK |
| 「开始扫码」报「扫码启动失败」 | `startScan` 传了 `lensFacing:'BACK'`，与插件枚举 `'Back'` 大小写不符直接抛错 | 启动页只传 `formats`，不再传非法枚举（v31 修复，在 `App/www/index.html`） |
| 权限列表里**根本没有相机**条目 | Capacitor 默认不声明 CAMERA 权限 → 哑包 | 构建时 Python 注入 Manifest 权限 + `aapt2` 硬校验（缺权限则构建失败，v32/v33） |
| 手填追溯码打开上传页、拍照键也卡死 | 3080 是 **http 非安全源**，安卓 WebView **不暴露网页相机**（getUserMedia 全死） | 拍照也改走 **原生 @capacitor/camera** 插件，彻底绕开 WebView 安全源限制（v33） |

**一句话结论**：App 内所有相机能力（扫码 + 拍照）一律走**系统原生相机**（`@capacitor/camera`），解码用**纯 JS 的 zxing**（无任何 Google 服务依赖）；NAS 端 3080 用 HTTP 直连免证书 —— 彻底避开 gms 依赖与 CameraX 预览兼容，全安卓机型通用。

---

## 三、最终版包含什么（权威源已校验）

### NAS 包（`photo-uploader-最终版-NAS.zip`）
- `server.js`（含 `APP_HTTP` 直连分支，第 900-903 行）
- `public/`（网页上传页；**`app.js` 已是 v33 原生相机版 61166 字节**，非旧的 54069 字节）
- `docker-compose.yml`（已含 `APP_HTTP=1`、`UPLOAD_BASE`、`TZ=Asia/Shanghai`）
- `Dockerfile`、`.dockerignore`、`.env.example`、`scripts/copy-lib.js`
- `deploy-nas.sh`（一键注入 + 重启 + 固化 + 验证）

### App 包（`photo-uploader-最终版-App.zip`）
- `package.json`（已加 `@capacitor/camera`）
- `capacitor.config.ts`（已加 `Camera` + `BarcodeScanner` 插件声明）
- `tsconfig.json`
- `.github/workflows/build-android.yml`（v4：拷贝 zxing UMD 库 → 加相机权限 → cap sync → 版本升 4.0 → 硬校验相机权限 → 出 APK）
- `www/index.html`（启动页，v4 改为系统相机拍照 + zxing 本地解码，彻底绕开 ML Kit 的 gms 依赖）
- `NAS_WEB_PATCH/`（NAS 端 `app.js` 等，备查/离线部署用）

---

## 四、重新部署步骤（两步，顺序别乱）

### 第 1 步：NAS 端
你已确认**可给 NAS 临时联网**，因此优先用「干净重建」；断网时再用「重注入」兜底。

**① 联网干净重建（推荐）** —— 删旧容器+旧镜像，从 Dockerfile 全新构建，彻底去掉历史 commit 残留，并重装齐 zxing-wasm/jpeg-js 等依赖：
```bash
cd <NAS包目录>
sudo -i
bash rebuild-nas.sh        # down → rmi → docker compose build（拉 node:18-alpine+装依赖）→ up -d → 验证 3080=200
```
构建完可断开网络，服务照常运行。

**② 离线重注入（保留旧镜像）** —— 往正在运行的容器注入最新文件并固化（断网也能用）：
```bash
cd <NAS包目录>
sudo -i
bash deploy-nas.sh         # docker cp 注入 server.js+public → restart → commit → 验证 3080=200
```

3. 验证：`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3080/` 返回 **200** 即成功。

> 注：之前不能「干净重建」是因为 NAS 离线时拉不到 `node:18-alpine` 基础镜像会失败；现在你能联网，第 ① 步即可放心用。若哪天又断网了，第 ② 步仍在。

### 第 2 步：App 端（GitHub Actions 云端构建，约 5 分钟）
1. 把 `App 包` 里的文件**上传/覆盖**到你自己的 GitHub 仓库根目录（保留目录结构：`www/`、` .github/workflows/`、`package.json`、`capacitor.config.ts`、`tsconfig.json`）。
2. push 到 `main`/`master` → Actions 自动跑 `Build Android APK`（或手动 Run workflow）。
3. 构建**必须**看到 `✅ 相机权限已包含在 APK 中`，否则构建标红，按日志修。
4. 下载产物 `photo-uploader-debug-apk` → 解压得 `app-debug.apk`。

### 第 3 步：装 + 验证
1. **先卸载手机旧 App**（避免版本混淆）。
2. 装新 APK → 「设置-应用信息」确认 **版本 4.0**、权限列表出现 **相机**。
3. 打开 App → 测试连接（填 `http://<NAS_IP>:3080`）→ **开始扫码**（调系统相机拍照 → 本地识别，对准条码自动跳上传页）。
4. 上传页点**拍照键** → 调起原生相机 → 拍完自动落库上传。
5. 查询输**后 8 位**追溯码命中。

---

## 五、临时自检（不等重打包，先确认 NAS 链路本身没问题）
手机/电脑浏览器开 **https://<NAS_IP>:3000**（接受证书警告）→ 这是安全源，网页相机可用 → 上传页拍照/识别都能跑。能跑通说明 NAS 端 OK，问题只在 App 端（装 4.0 即可解决）。

---

## 六、常见排错
- **3080 仍 301 跳 3000**：容器 env 没 `APP_HTTP=1`（检查 `docker inspect` + compose），或注入的是旧 server.js（确认是第 901 行带 `APP_HTTP` 分支的）。
- **扫码黑屏 / 拍照卡死**：装的还是旧 APK（ML Kit 黑屏 / 无 CAMERA 权限）。确认版本 4.0 + 权限有相机，重装。新方案已彻底去掉 ML Kit 的 gms 依赖，国产 ROM 不再黑屏。
- **构建报 invalid source release: 21**：workflow 已自动切 JDK 21，若你改过请保留 `setup-java` 的 `java-version: '21'`。
- **zxing-wasm 警告 / 服务端解码不可用**：进容器 `docker exec -it fnnas-photo-uploader npm install zxing-wasm jpeg-js` 再重启（镜像已固化则可跳过）。
